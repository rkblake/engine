function ready()
    -- spawn("ball.lua")
    -- spawn_at("paddle.lua", x=50, y=00)
    -- spawn_at("paddle.lua", x=750, y=300)
end

function update()
    draw_text("hello", 100, 100)
    draw_rect(0,0,100,100)
end

function key_input(key, pressed)

end

function mouse_input(x, y, click, button_index)

end
